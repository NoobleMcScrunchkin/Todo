---
---

<div class="kanban-board">
	<div class="kanban-board-header">Kanban Board</div>
	<div class="board">
		{
			[...Array(Math.floor(Math.random() * 8) + 2).keys()].map((obj, index) => (
				<div class="col">
					<div class="col-header">Section Name</div>
					<div class="col-tasks no-scrollbar">
						{[...Array(Math.floor(Math.random() * 40) + 5).keys()].map((obj, index) => (
							<div class="task">Example Task Card</div>
						))}
					</div>
				</div>
			))
		}
	</div>
</div>
