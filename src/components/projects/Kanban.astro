---
---

<div class="w-[calc(100%-16rem)] h-full py-2 px-6">
  <div class="flex flex-col h-full">
    <div class="text-2xl mt-2">Kanban Board</div>
    <div class="flex-grow w-full flex flex-row overflow-x-scroll gap-2 mt-4">
      {
        [...Array(Math.floor(Math.random() * 8) + 2).keys()].map(
          (obj, index) => (
            <div class="h-full w-80 min-w-[20rem] flex flex-col bg-zinc-100 rounded">
              <div class="w-full p-2 text-sm uppercase font-bold">
                Section Name
              </div>
              <div class="w-full flex-grow p-2 flex flex-col gap-2 overflow-y-scroll no-scrollbar">
                {[...Array(Math.floor(Math.random() * 40) + 5).keys()].map(
                  (obj, index) => (
                    <div class="w-full px-4 py-2 text-sm bg-white rounded">
                      Example Task Card
                    </div>
                  )
                )}
              </div>
            </div>
          )
        )
      }
    </div>
  </div>
</div>
